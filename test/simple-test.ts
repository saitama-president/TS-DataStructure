import {
    assert
} from 'chai';
import { Relation } from '../src/Relation';



describe('Relation Test', () => {
    console.dir("TEST BEGIN");

    it("IS FIRST TEST",()=>{
        assert.isTrue(true);
    });

    console.dir("TEST END");
});
