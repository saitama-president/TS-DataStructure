import { assert } from "chai";
import { Relation } from "../src/Relation";
import { Arm } from "../src/Arm";

describe("Sample Test", () => {
  it("EMPTY TEST", () => {
    assert.isTrue(true);
  });
});
